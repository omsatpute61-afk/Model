# Training / evaluation / export (workstation or server).
torch>=2.1
torchvision>=0.16
numpy>=1.24
Pillow>=10.0
pyyaml>=6.0            # YAML configs
onnx>=1.15             # ONNX export
onnxruntime>=1.17      # export verification, INT8 quantisation, edge inference
onnxscript>=0.1        # required by torch>=2.6's ONNX exporter
pytest>=7.4            # tests
